#!/usr/bin/env node
/** CLI dispatcher — prefer package.json scripts pointing at setup-schemas.ts / publisher.ts */

console.log('Use: pnpm sds-publish:setup-schemas | pnpm sds-publish:run | pnpm sds-publish:read <bountyId>');
